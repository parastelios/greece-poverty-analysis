# Greece Poverty Analysis

A data-driven investigation into why Greek households report the EU's highest
subjective financial strain — a 52.6-point gap, averaged over 2015–2024,
between the share reporting difficulty making ends meet (1st of 27) and the
official income-poverty rate, AROP (7th of 27) — and how much of that gap the
available evidence can actually account for.

**Read [`docs/project_description.md`](docs/project_description.md) first.**
It is the current, complete description of the finished project — the two
questions it answers, every subquestion under them, the evidence hierarchy,
and what remains unexplained — written for a reader who wants the completed
argument, not the chronological process that produced it.

**This is the V2 rewrite**, developed on `p6-rewrite` and now merged to
`main`, which both branches track in lockstep. It replaces an earlier, more
confident V1 draft after review found several of its claims overstated; V2
is built around a pre-registered testing protocol (declared conditions,
multiplicity correction, wild-cluster bootstrap, minimum-detectable-effect
reporting) so a verdict is the output of a fixed procedure rather than a
judgement made after seeing the numbers. It answers with the same discipline
the V1 draft lacked: a broader official measure narrows the gap but doesn't
close it; three present-day conditions and three accumulated ones each add
real information beyond income poverty; and *most of the 52.6 points remains
unattributed*, because the analysis could not establish where it goes. The
V1 draft is preserved as-is in `archive/v1-final/` for comparison, not
deleted or silently superseded.

Two documents both describe "the project" and answer different questions.
[`docs/project_description.md`](docs/project_description.md) describes what
V2 *is*, as completed — read this first.
[`docs/archive/pre-v2-publication/project_description_v3.md`](docs/archive/pre-v2-publication/project_description_v3.md)
is the pre-registered *protocol* V2 was built against — the brief that fixed
each construct's testing conditions before any outcome was seen. It is filed
alongside superseded status documents because it no longer describes current
status, but it is still the live citation behind several pipeline scripts'
pre-registration comments, and is kept in place rather than rewritten for
that reason.

## The published documents

| Document | What it is |
|---|---|
| [`output/v2_report.html`](output/v2_report.html) | The main technical report: the eight-stage argument, every frozen claim with its evidence tier and caveats, and the fifteen main-path figures. |
| [`output/academic_paper.html`](output/academic_paper.html) | The same evidence base in academic register, for a reader who wants the argument without the report's guided narration. |
| [`output/narrative.html`](output/narrative.html) | A narrative companion: the same evidence, told as one continuous story rather than a section-by-section technical document. |
| [`output/statistical_appendix.html`](output/statistical_appendix.html) | Every variable used anywhere in the project, interactive, against all 27 member states — including figures the main report simplified away, the technical diagnostics behind its conclusions, and the full context register in a form the other three documents don't carry. |
| [`docs/v2_research_record.md`](docs/v2_research_record.md) (also built as [`output/research_record.html`](output/research_record.html)) | The live research log: every stage, every correction made during review and why, every artifact this project produced. This is the current source of truth for project status — nothing else in this repo should be read as a status report. |

None of the four published documents are hand-edited; all are generated by
`scripts/88_assemble_report.py`, `90_build_paper.py`, `91_build_narrative.py`
and `47_build_appendix.py` respectively, from the same frozen claim set and
context register. PDF exports of all four live under `output/pdf/`,
regenerated by `scripts/make_pdfs.py` and committed alongside the HTML they
were built from.

## Data and reproducibility

```bash
make verify
```

Runs the full test suite plus three independent gates: every headline number
in the published documents against what the pipeline actually produced, every
figure's payload against its report and appendix copies (109 checks), and
every frozen claim and context-register entry against where it's required to
appear. `make release-verify` adds the stricter checks needed before shipping
a release (no pending claim, no unfilled document slot); `python3
scripts/verify_report_v3.py` is the report's own atomic acceptance gate
(structure, claim containers, caveats, generated-table parity).

**Data freshness is not uniform across this project, deliberately.** The
estimation panel behind every frozen claim (`data/raw/`, `data/processed/`)
is a cached snapshot, fetched at known points and re-used so the frozen
results don't silently drift between runs — see
`docs/v2_research_record.md`'s protocol section for what's in it and how
missing data is handled, rather than a hardcoded date here that would only
go stale again. The statistical appendix's own descriptive comparison series
(`scripts/46_appendix_data.py`) are the deliberate exception: they call the
live Eurostat API on every build, so Eurostat's own periodic revisions reach
the appendix's charts without a manual re-fetch step. Neither policy affects
the frozen claim set.

```bash
make fetch          # re-acquire raw inputs, CHECK mode: compares, writes nothing
make fetch-write    # same, but overwrites data/raw/ for real
make build          # rerun the full pipeline over existing data/raw/, then verify
make reproduce      # true end-to-end test: fresh clone, live fetch, build, verify — leaves this working copy untouched
```

## Setup

```bash
pip install -r requirements.txt
```

Python 3.11+ recommended (dependencies pinned in `requirements.txt`). No API
key needed — the Eurostat and ELSTAT dissemination APIs used here are public.

## Project structure

```
scripts/    All analysis and build code, run from inside this directory.
            Numbered in dependency order, but BUILD ORDER IS NOT FILENAME
            ORDER — two scripts write derived columns back into an earlier
            script's output table, and the Makefile hoists them accordingly.
            The `build` target in the Makefile is the authoritative build
            order; don't infer it from filenames alone.
data/raw/          Cached raw Eurostat/ELSTAT pulls, one file per dataset/query.
data/processed/    Every derived artifact: merged panels, per-figure CSVs,
                   the frozen claim set (e_final_claims.csv) and the context
                   register (context_register.csv).
docs/              project_description.md (the current, completed-project
                   description — read this first), methodology references
                   (comparability_notes.md, data_sources.md), the live
                   research record (v2_research_record.md), the
                   health-extension writeup, and archive/pre-v2-publication/
                   for superseded planning and status documents kept for
                   provenance, not current use — except project_description_v3.md
                   there, the pre-registered protocol V2 was built against, which is
                   still a live citation, not just historical record.
output/            The four published documents (above), their PDF exports,
                   and the intermediate build/ pages they're assembled from.
archive/v1-final/  The V1 draft, frozen as published, kept for comparison.
```

## Notes

- `eurostat.py`'s `fetch()` is a general-purpose SDMX-JSON parser, reusable
  for any Eurostat dataset, not specific to this project.
- Several scripts fetch cross-country panels independently rather than
  sharing one big pull — each is scoped to what its own analysis needs,
  which means a full rebuild from raw data takes a few minutes of API calls.
- `docs/v2_research_record.md` records what's been tried, what came back
  null, and every correction made during review, with its reasoning. Treat
  it as the first place to look for "what is the current state of this
  project," not this file.
